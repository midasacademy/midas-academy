<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Midas Academy | Fees Management</title>
    <link rel="stylesheet" href="../stylesheets/dashboard.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.4.0/fonts/remixicon.css" rel="stylesheet">
    <style>
        #left #links .link:nth-child(1) {
            color: #00000085;
        }

        #left #links .link:nth-child(4) {
            color: #00000085;
        }

        #left #links .link:nth-child(5) {
            color: #000;
        }

        #left #links .link:nth-child(6) {
            color: #00000085;
        }
    </style>
</head>

<body>
    <div id="main">

        <% include ./partials/nav.ejs %>

        <div id="right">
            <div style="display: flex; flex-direction: column;" class="dashboard">
                <div class="fees">
                    <h2>Fee Management</h2>
                    <form action="/feesubmit" method="post">
                        <label for="student">Select Student:</label>
                        <select id="student" name="student">
                            <% users.forEach(function(user){ %>
                                <option value="<%= user.name %>"><%= user.name %></option>
                            <% }) %>
                        </select> 
                        <label for="search_student">or Search:</label>
                        <input type="number" id="search_student" name="search_student">
                        <hr style="margin: 20px; border: none;">
                        <label for="fee_amount">Fee Amount:</label>
                        <input type="number" id="fee_amount" name="fee_amount" required>
                        <hr style="margin: 20px; border: none;">
                        <button type="submit">Submit</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>

</html>